const namePattern = /^[a-zA-z]+([\s][a-zA-Z]+)*$/
const dataPattern =  /^[a-zA-z]*$/
const bloodGroupPattern = /(A|B|AB|O)[+-]/

module.exports={
    namePattern,
    dataPattern,
    bloodGroupPattern
}